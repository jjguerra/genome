# genome

usage: python vcf.py [-h] [-d DIRECTORY]

optional arguments:
  -h, --help            show this help message and exit
  -d DIRECTORY, --directory DIRECTORY, --dir DIRECTORY
                        directory location of the samples
